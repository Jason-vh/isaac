services:
  db:
    image: postgres:17
    ports:
      - "5434:5432"
    environment:
      POSTGRES_DB: isaac
      POSTGRES_USER: isaac
      POSTGRES_PASSWORD: isaac
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
